os:
  - linux
  - osx
  
language: go

go:
  - master

before_install:
  # - go get -u github.com/golang/dep/cmd/dep
  # - dep ensure -v

after_success:
  - ls -laF
  - tar cvzf mocli.tar.gz mocli
  - ls -laF
